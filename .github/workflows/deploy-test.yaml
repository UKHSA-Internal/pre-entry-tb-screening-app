name: Pets Deployment - Test

on:
  workflow_dispatch:
  pull_request:
    types: [closed]
    branches: [develop]

jobs:
  deploy:
    uses: ./.github/workflows/deploy.yaml
    with:
      environment: "qat"
    secrets: inherit
