name: Pets UI Deployment - Test

on:
  workflow_dispatch:
  push:
    branches: [feature/TBBETA-476]

jobs:
  pets-ui:
    uses: ./.github/workflows/app-deploy.yaml
    with:
      environment: "test"
    secrets: inherit
